# 景田赛事竞猜活动 - 前台 PRD

## 1. 文档信息

| 项目 | 内容 |
| --- | --- |
| 文档名称 | 景田赛事竞猜活动 - 前台 PRD |
| 当前版本 | V1.2 |
| 文档日期 | 2026-05-07 |
| 适用范围 | 微信小程序内嵌活动页、当前静态 HTML 原型 |
| 原型文件 | `index.html` |
| 本次调整重点 | 收拢页面模块、弹窗、状态和排序规则，减少分散描述，便于研发按模块实现 |

## 2. 项目背景

景田赛事竞猜活动用于公司内部赛事期间的互动运营。1.0 阶段先面向公司内部试点，运行在微信小程序内嵌活动页中，通过收集小程序 AppID 并加入白名单控制访问范围。后续 2.0 可能面向更大范围用户开放，但本 PRD 不展开 2.0 规则。

活动通过“赚积分、参与竞猜、积分抽奖、奖品承接”的路径提升赛事参与感。用户未直接参赛，也可以通过竞猜、抽奖和奖励反馈参与活动。

## 3. 项目目标

- 提升赛事期间用户参与感，让非参赛用户也能围绕赛事互动。
- 用任务和积分引导用户进入活动、参与竞猜、消耗积分。
- 用竞猜结果、抽奖结果和连中奖励形成及时反馈。
- 为运营提供可配置、可统计、可承接奖品发放的前台口径。

## 4. 名词说明

| 名词 | 说明 |
| --- | --- |
| 可用积分 | 用户当前可用于竞猜下注、积分抽奖或兑换的积分余额。 |
| 竞猜 | 用户选择一场未开始比赛中的一支队伍，并投入积分支持。 |
| 竞猜截止时间 | 通用规则为比赛日期前一日 `23:59:59` 截止；例如 6.2 日比赛，竞猜截止时间为 6.1 日 `23:59:59`。 |
| 已参与竞猜 | 用户已对某场比赛提交竞猜，不能重复竞猜。 |
| 待开奖 | 比赛已截止或已结束，但赛果尚未结算。 |
| 已猜中 | 用户支持队伍与最终胜出队伍一致。 |
| 已猜错 | 用户支持队伍与最终胜出队伍不一致。 |
| 连中奖励 | 用户连续 N 场已参与且已结算的竞猜猜中后获得的奖励，N 支持后台配置，默认 3。 |
| 我的奖品 | 承接用户通过抽奖、连中奖励等获得的奖品记录，当前原型只保留入口占位。 |

## 5. 项目范围

### 5.1 本期包含

- 赛事竞猜首页。
- 顶部资产栏与任务中心入口。
- 赛事日期 Tab、竞猜卡片、竞猜规则入口、我的竞猜记录入口。
- 竞猜下注弹窗与竞猜结果通知弹窗。
- 积分抽奖区、抽奖中奖弹窗、抽奖未中奖弹窗。
- 积分兑换入口。
- 我的奖品悬浮入口。
- 连中奖励弹窗。

### 5.2 本期不包含

- 后台管理系统详细需求。
- 真实奖品发放组件页面。
- 扫码核销、兑换码核销。
- 真实支付、物流、库存履约。
- 2.0 面向公开用户后的风控和奖品监管方案。

## 6. 页面结构总览

### 6.1 首页区域顺序

首页采用单页纵向滚动结构，模块顺序如下：

1. 顶部资产栏。
2. 赛事竞猜区。
3. 积分抽奖区。
4. 积分兑换入口。
5. 我的奖品悬浮入口。

### 6.2 用户主路径

1. 用户进入活动页，先看到活动标题、可用积分和领积分入口。
2. 用户在赛事竞猜区选择有比赛的日期，查看比赛卡片。
3. 用户点击可竞猜比赛，选择支持队伍并投入积分。
4. 赛果结算后，用户收到竞猜结果通知，并可通过“我的竞猜记录”查看输赢和积分变化。
5. 用户可消耗积分抽奖，中奖结果进入“我的奖品”承接。
6. 用户连续 N 场猜中后，收到连中奖励弹窗，奖励进入“我的奖品”承接。

## 7. 首页模块详细说明

### 7.1 顶部资产栏

#### 模块目标

让用户进入页面后立即知道活动主题、当前可用积分，以及如何获取积分。

#### 展示内容

| 字段/元素 | 展示规则 |
| --- | --- |
| 活动标题 | 展示“赛事竞猜”。 |
| 我的可用积分 | 展示用户当前可用积分；积分为 0 时显示 `0`。 |
| 领积分入口 | 展示为按钮，点击打开任务中心弹窗。 |

#### 交互规则

- 点击“领积分”打开任务中心弹窗。
- 任务完成后，顶部积分余额需要同步刷新。

#### 异常与空态

- 积分加载失败时，积分位置可显示 `--`，并提示“积分加载失败，请稍后重试”。
- 用户未在白名单内时，不进入活动页，展示无权限提示页或小程序统一无权限提示。

### 7.2 赛事竞猜区

#### 模块目标

承载本活动核心参与路径，让用户按日期查看比赛、判断能否竞猜、完成竞猜并回看参与记录。

#### 展示内容

| 元素 | 展示规则 |
| --- | --- |
| 模块标题 | 展示“赛事竞猜”。 |
| 竞猜规则入口 | 位于标题区右侧，点击打开竞猜规则弹窗。 |
| 我的竞猜记录入口 | 位于标题区右侧，点击打开我的竞猜记录弹窗。 |
| 日期 Tab | 横向展示有比赛的日期。 |
| 竞猜卡片列表 | 根据当前选中日期展示对应比赛卡片。 |

#### 日期 Tab 规则

- 日期不是每天都展示，只有当天有比赛时才展示该日期。
- 当天如果没有比赛，不展示当天日期。
- 默认选中距离当前最近且有比赛的日期。
- 所有日期均无比赛时，不展示空日期 Tab，赛事区展示“暂无赛事安排”。
- 日期 Tab 文案建议使用 `6.1日`、`6.2日` 这类短日期格式。

#### 竞猜截止时间规则

- 竞猜截止时间采用通用规则：比赛日期前一日 `23:59:59` 截止。
- 例如比赛日期为 `6.2日`，则用户最晚可在 `6.1日 23:59:59` 前完成竞猜；从 `6.2日 00:00:00` 起不可继续竞猜。
- 截止时间按活动统一时区判断，1.0 默认按北京时间处理。
- 竞猜截止后，未参与用户仍可看到该场比赛卡片，但不能再打开下注弹窗。

#### 竞猜卡片字段

| 字段 | 未参与时 | 已参与/已结算时 |
| --- | --- | --- |
| 比赛双方 | 展示双方队伍名称。 | 展示双方队伍名称。 |
| VS | 双方队伍中间展示 `VS`。 | 双方队伍中间展示 `VS`。 |
| 竞猜入口 | 展示“点击参与竞猜”。 | 不展示参与入口。 |
| 已支持队伍 | 不展示。 | 展示“已支持 XX”或“我支持：XX”。 |
| 投入积分 | 不展示。 | 展示“已投入 X 积分”。 |
| 当前状态 | 可不展示状态标签。 | 展示待开奖、已猜中、已猜错、已取消/已退款等状态标签。 |
| 结果说明 | 不展示。 | 已猜中可展示获得积分；已取消可展示退款积分。 |

#### 竞猜卡片排序

同一日期内卡片按状态排序，推荐顺序如下：

1. 未参与竞猜且仍可竞猜。
2. 已参与竞猜/待开奖。
3. 已截止未参与。
4. 已猜中/已猜错。
5. 已取消/已退款。

同一状态内，按比赛开始时间从早到晚排序。若比赛开始时间暂未展示，仍需要后台返回排序字段，前台按该排序展示。

#### 竞猜卡片状态

| 状态 | 卡片展示 | 用户是否可竞猜 | 点击行为 |
| --- | --- | --- | --- |
| 未参与竞猜 | 展示双方队伍、`VS`、`点击参与竞猜`。 | 是 | 点击打开竞猜下注弹窗。 |
| 已参与竞猜 | 展示双方队伍、已支持队伍、已投入积分、`待开奖`。 | 否 | 点击不打开下注弹窗，可提示“本场已参与竞猜”。 |
| 已截止未参与 | 展示双方队伍、`VS`、`竞猜已截止`，入口置灰。 | 否 | 点击提示“当前比赛已不可竞猜”。 |
| 待开奖 | 展示已支持队伍、投入积分、`待开奖`。 | 否 | 点击可提示“赛果待公布”。 |
| 已猜中 | 展示已支持队伍、投入积分、`已猜中`，可展示获得积分。 | 否 | 点击可查看结果反馈或保持无操作。 |
| 已猜错 | 展示已支持队伍、投入积分、`已猜错`。 | 否 | 点击可查看结果反馈或保持无操作。 |
| 已取消/已退款 | 展示`已取消/已退款`，如用户已参与则展示退款积分。 | 否 | 点击可提示“比赛已取消，积分已退回”。 |

#### 交互规则

- 只有“未参与竞猜”且未超过竞猜截止时间的比赛可打开下注弹窗。
- 每场比赛每个用户只能竞猜一次。
- 已参与、已截止未参与、待开奖、已猜中、已猜错、已取消/已退款状态均不可再次下注。
- 用户竞猜成功后，卡片立即切换为已参与状态，并刷新顶部积分余额。
- 用户竞猜成功后，自动完成任务中心里的“今日竞猜一次”任务。

#### 异常与空态

- 当前选中日期无比赛时，展示“暂无赛事安排”。
- 比赛列表加载失败时，展示“赛事加载失败，请稍后重试”。
- 用户余额不足时，不进入确认提交，提示“当前积分不足”。

### 7.3 积分抽奖区

#### 模块目标

提供积分消耗路径，通过奖品预览、中奖公告和明确消耗值提升抽奖转化。

#### 展示内容

| 元素 | 展示规则 |
| --- | --- |
| 模块标题 | 展示“积分抽奖”。 |
| 单次消耗 | 展示“50积分/次”或“每次抽奖消耗 50 积分”。 |
| 中奖公告 | 展示静态或后台返回的中奖公告，例如“恭喜李** 抽中赛事周边礼包”。 |
| 热门奖品预览 | 横向展示奖品图片或占位图、奖品名称。 |
| 立即抽奖按钮 | 点击后触发抽奖结果。 |

#### 抽奖状态与处理

| 状态 | 前台处理 |
| --- | --- |
| 可抽奖 | 用户积分大于等于单次消耗，点击后扣除积分并展示中奖或未中奖弹窗。 |
| 积分不足 | 不扣积分，不打开结果弹窗，Toast 提示“当前积分不足”。 |
| 未中奖 | 展示未中奖弹窗，用户可点击“再抽一次”。 |
| 抽中奖品 | 展示中奖弹窗，奖品进入“我的奖品”。 |
| 已达中奖上限 | 本次活动每人最多中 1 个抽奖奖品；已中奖用户再次点击不扣积分，Toast 提示“本次活动每人最多中奖 1 次”。 |

#### 交互规则

- 点击“立即抽奖”前需要校验积分余额。
- 未中奖不返还积分。
- 已中奖用户再次抽奖不扣积分。
- 抽奖奖品图片在当前原型中可用占位图，正式版本由后台配置或素材配置提供。

#### 异常与空态

- 奖品预览为空时，展示“奖品准备中”。
- 中奖公告为空时，可隐藏公告区域，不展示空白条。

### 7.4 积分兑换入口

#### 模块目标

为积分商城或后续兑换组件提供入口，承接用户消耗积分兑换奖品的需求。

#### 展示内容

- 展示“积分兑换”模块标题。
- 展示进入积分商城的入口卡片或按钮。
- 文案需要让用户理解这是兑换入口，不与抽奖入口混淆。

#### 交互规则

- 点击入口跳转到积分商城或打开复用兑换组件。
- 当前原型可用 Toast 或占位行为表示。

#### 异常与空态

- 兑换组件不可用时，提示“积分商城暂未开放”。

### 7.5 我的奖品悬浮入口

#### 模块目标

承接抽奖中奖、连中奖励等用户已获得奖品，提供统一查看入口。

#### 展示内容

- 首页右下侧展示“我的奖品”悬浮按钮。
- 按钮需要悬浮在内容区上方，不占用列表流。

#### 奖品状态口径

| 状态 | 用户理解 |
| --- | --- |
| 待发放 | 奖品已记录，等待运营发放。 |
| 已发放 | 奖品已完成发放。 |
| 发放失败 | 奖品发放异常，需运营处理。 |
| 无需发放 | 如谢谢参与、未中奖等不产生奖品记录。 |

#### 交互规则

- 点击“我的奖品”进入或复用我的奖品组件。
- 当前原型点击后 Toast 提示“这里复用我的奖品组件”。
- 抽奖中奖和连中奖励均应进入“我的奖品”承接。

## 8. 弹窗详细说明

### 8.1 通用弹窗规则

- 弹窗统一使用遮罩承载。
- 可关闭弹窗应支持点击右上角 `x`、点击空白遮罩或点击主按钮关闭。
- 涉及结果反馈的弹窗，主按钮文案需要明确，例如“我知道了”“开心收下”“再抽一次”。
- 弹窗关闭后不得丢失已完成的积分、竞猜或奖品状态。

### 8.2 任务中心弹窗

#### 触发入口

点击顶部“领积分”按钮打开。

#### 展示内容

| 任务 | 奖励 | 完成规则 |
| --- | --- | --- |
| 每日签到 | +20 积分 | 点击“去完成”后完成。 |
| 今日竞猜一次 | +30 积分 | 用户成功提交一次竞猜后自动完成。 |
| 宠物寻宝一次 | +20 积分 | 当前原型点击后模拟完成。 |
| 使用一次 AI 客服对话 | +20 积分 | 当前原型点击后模拟完成。 |

#### 操作按钮

- 未完成任务展示“去完成”。
- 已完成任务展示“已完成”，不可重复领取。

#### 关闭方式

点击 `x` 或空白遮罩关闭。

#### 异常状态

- 任务奖励发放失败时提示“领取失败，请稍后重试”。
- 已完成任务再次点击，不重复发放积分。

### 8.3 竞猜规则弹窗

#### 触发入口

点击赛事竞猜区标题右侧“竞猜规则”打开。

#### 展示内容

- 参与方式：选择未开始的比赛，支持一方队伍并投入积分。
- 每场比赛每个用户仅可竞猜一次。
- 投入积分不得超过当前可用积分。
- 竞猜截止时间为比赛日期前一日 `23:59:59`；例如 6.2 日比赛，6.1 日 `23:59:59` 后不可继续竞猜。
- 猜中后按活动规则发放奖励积分。
- 猜错后投入积分不返还。
- 比赛取消后，已投入积分原路退回。
- 连中奖励说明：连续 N 场已参与且已结算的竞猜猜中后，可获得连中挑战奖励，N 由后台配置。

#### 操作按钮

主按钮展示“我知道了”。

#### 关闭方式

点击 `x`、空白遮罩或“我知道了”关闭。

#### 异常状态

规则文案加载失败时，可展示默认规则文案。

### 8.4 我的竞猜记录弹窗

#### 触发入口

点击赛事竞猜区标题右侧“我的竞猜记录”打开。

#### 空态

- 无竞猜记录时展示“暂无竞猜记录”。
- 辅助文案展示“参与赛事竞猜后将在这里展示”。

#### 记录字段

| 字段 | 说明 |
| --- | --- |
| 比赛日期 | 展示比赛所属日期。 |
| 对阵双方 | 展示双方队伍名称，例如“深圳 VS 广州”。 |
| 我支持的队伍 | 展示用户已选择支持的队伍。 |
| 投入积分 | 展示本场竞猜投入积分。 |
| 当前结果状态 | 展示待开奖、已猜中、已猜错、已取消/已退款。 |
| 可选扩展字段 | 赛果、获得积分、退款积分、结算时间。 |

#### 状态标签

| 状态 | 展示文案 |
| --- | --- |
| 未结算 | 待开奖 |
| 猜中 | 已猜中 |
| 猜错 | 已猜错 |
| 比赛取消且已退款 | 已取消/已退款 |

#### 记录排序

1. 未结算记录优先展示。
2. 已结算记录排在未结算记录之后，按结算时间倒序展示。
3. 已取消/已退款记录排在已结算记录之后。
4. 同一排序条件下，按用户参与时间倒序展示。

#### 交互规则

- 记录仅查看，不支持编辑、撤回、改投或再次下注。
- 点击记录本身不进入详情页。
- 若后续需要详情页，应单独补充需求。

#### 关闭方式

点击 `x` 或空白遮罩关闭。

### 8.5 竞猜下注弹窗

#### 触发入口

点击“未参与竞猜”且未超过竞猜截止时间的比赛卡片。

#### 展示内容

| 字段 | 说明 |
| --- | --- |
| 比赛双方 | 展示当前比赛双方队伍。 |
| 当前可用积分 | 展示用户当前余额，辅助判断能否下注。 |
| 最小投入积分 | 展示后台配置的最小下注积分，默认 `10`。 |
| 单场最大投入上限 | 展示后台配置的单场最大下注上限；后台未配置时展示“不超过当前可用积分”。 |
| 支持队伍 | 用户选择队伍 A 或队伍 B，选中后高亮展示。 |
| 投入积分输入区 | 由减号按钮、数字输入框、加号按钮组成。 |
| 确认按钮 | 文案为“确认投入”或“确认支持”。 |

#### 后台配置映射

| 后台配置 | 前台使用方式 |
| --- | --- |
| 最小下注积分 | 用于弹窗默认投入积分、减号下限、输入校验和错误提示；默认 `10`。 |
| 单场最大下注上限 | 用于计算最大可投积分；可为空。 |
| 同场下注限制 | 同一用户同一比赛只能下注一次，不支持加注、改投、撤回。 |

#### 投入积分默认值

- 默认投入积分取后台配置的最小下注积分。
- 如果用户可用积分低于最小下注积分，弹窗仍可打开，但确认按钮置灰，并提示“当前积分不足”。
- 最大可投积分 = `min(用户可用积分, 后台单场最大下注上限)`。
- 如果后台未配置单场最大下注上限，最大可投积分 = 用户可用积分。

#### 投入积分交互

| 操作 | 交互规则 |
| --- | --- |
| 点击减号 | 每次减少一个步进值，最低不能低于最小下注积分。 |
| 点击加号 | 每次增加一个步进值，最高不能超过最大可投积分。 |
| 手动输入 | 只允许输入正整数；失焦或点击确认时校验。 |
| 步进值 | 默认等于后台配置的最小下注积分，默认 `10`。 |

#### 输入校验与提示

| 异常情况 | 前台提示 |
| --- | --- |
| 输入为空、0、小数、负数、非数字 | `请输入有效积分` |
| 投入积分低于最小下注积分 | `最低投入 X 积分` |
| 投入积分超过用户可用积分 | `当前积分不足` |
| 投入积分超过单场最大下注上限 | `单场最多可投入 X 积分` |

#### 确认按钮状态

| 状态 | 按钮表现 | 点击提示 |
| --- | --- | --- |
| 未选择支持队伍 | 置灰或不可提交 | `请选择支持队伍` |
| 投入积分不合法 | 置灰或不可提交 | 按具体校验结果提示。 |
| 用户可用积分低于最小下注积分 | 置灰或不可提交 | `当前积分不足` |
| 比赛已截止 | 不可提交 | `当前比赛已不可竞猜` |
| 比赛已取消 | 不可提交 | `比赛已取消，无法参与竞猜` |
| 用户已参与本场比赛 | 不可提交 | `本场已参与竞猜，不能重复参与` |
| 所有条件满足 | 可点击 | 点击后进入提交中状态。 |

#### 成功反馈

- 扣除投入积分并刷新顶部可用积分。
- 关闭弹窗。
- 对应比赛卡片切换为已参与状态。
- 我的竞猜记录新增该场竞猜记录。
- 自动完成“今日竞猜一次”任务。

#### 提交异常

- 提交中按钮展示“提交中”，防止重复点击。
- 提交时余额变化导致不足，提示“当前积分不足，请重新调整投入积分”。
- 提交时比赛状态变化导致不可下注，提示“当前比赛已不可竞猜”。
- 提交时已超过竞猜截止时间，提示“当前比赛已不可竞猜”。
- 提交时用户已参与本场比赛，提示“本场已参与竞猜，不能重复参与”。
- 网络或服务异常，提示“提交失败，请稍后重试”，不扣前台展示积分。

### 8.6 竞猜结果通知弹窗

#### 触发入口

用户再次进入活动页或前台收到结算结果后，如存在未读竞猜结果，弹出竞猜结果通知弹窗。

#### 展示内容

- 标题：`竞猜结果已出`。
- 文案：`您参与的竞猜已有结果，具体结果和积分变化可前往「我的竞猜记录」查看。`
- 不在该弹窗内区分猜中、猜错、取消退款；具体状态、投入积分、获得积分、退款积分、结算时间统一在“我的竞猜记录”中展示。

#### 操作按钮

- 主按钮展示`查看竞猜记录`。
- 点击后关闭当前通知弹窗，并打开“我的竞猜记录”弹窗。

#### 关闭方式

- 点击`查看竞猜记录`：关闭当前通知弹窗，并打开“我的竞猜记录”弹窗。
- 点击`x`或空白遮罩：仅关闭当前通知弹窗，不自动打开“我的竞猜记录”。

#### 异常状态

- 如果有多条未读竞猜结果，只弹出一次统一通知；所有明细在“我的竞猜记录”中展示。
- 同一批未读结果关闭或点击查看后，不再重复弹出；竞猜记录仍长期保留。
- 没有未读竞猜结果时，不弹出结果通知弹窗。

### 8.7 抽奖中奖弹窗

#### 触发入口

用户点击“立即抽奖”并抽中奖品后打开。

#### 展示内容

- 标题：`恭喜中奖`。
- 奖品图片或占位图。
- 奖品名称。
- 发放说明：奖品已记录，请前往“我的奖品”查看或以后续发放通知为准。

#### 操作按钮

主按钮展示“开心收下”。

#### 关闭方式

点击主按钮、`x` 或空白遮罩关闭。

#### 异常状态

中奖结果已生成但展示失败时，应以“我的奖品”记录为准。

### 8.8 抽奖未中奖弹窗

#### 触发入口

用户点击“立即抽奖”并未中奖后打开。

#### 展示内容

- 标题：`很遗憾未中奖`。
- 正文：`差一点就中了~`、`很可惜，与大奖擦肩而过`。

#### 操作按钮

只展示一个主按钮：`再抽一次`。

#### 关闭方式

点击空白遮罩可关闭。

#### 异常状态

- 点击“再抽一次”时，如果积分不足，Toast 提示“当前积分不足”。
- 积分不足时不扣积分，不打开新的结果弹窗。

### 8.9 连中奖励弹窗

#### 触发入口

用户连续 N 场已参与且已结算的竞猜猜中后打开，N 由后台配置，默认 3。

#### 展示内容

- 标题：`太稳了！N 连中达成`。
- 奖品图片占位。
- 奖励说明：`连中挑战奖励`。
- 奖品名称：`水瓶挂件`。
- 引导文案：`奖励已发放至「我的奖品」`。

#### 操作按钮

主按钮展示“我知道了”。

#### 关闭方式

点击主按钮、`x` 或空白遮罩关闭。

#### 业务规则

- 只统计用户已参与且已结算的竞猜记录。
- 猜中累计连中次数，猜错清零。
- 未参与比赛不打断连中。
- 比赛取消/退款不计入，也不打断连中。
- 每个用户每次活动最多获得 1 次连中奖励。
- 达成后生成奖品记录，进入“我的奖品”承接。

## 9. 跨模块业务规则

### 9.1 白名单访问

1.0 公司内部试点通过小程序 AppID 白名单控制访问范围。用户不在白名单时，不进入活动首页。

### 9.2 积分口径

- 前台展示积分余额，以后台返回结果为准。
- 竞猜下注、抽奖消耗、任务奖励、竞猜结算、取消退款均需要刷新可用积分。
- 前台只展示用户可感知结果，不展示账务流水明细。

### 9.3 我的奖品承接

- 抽奖中奖、连中奖励均进入“我的奖品”承接。
- 1.0 不做扫码核销，不展示兑换码。
- 公司内部试点如需统计发放数量，由后台获奖名单或导出记录承接。

### 9.4 连中奖励边界

- 连中奖励属于参与激励，不属于竞猜积分结算。
- 连中奖励门槛 N 支持后台配置，默认 3。
- 配置变更只影响后续未达成用户，不回溯已获得奖励用户。

### 9.5 2.0 风险边界

如果后续面向公开用户开放，连续竞猜直接发实物奖励存在监管与成本风险。2.0 建议优先调整为积分奖励或抽奖资格，不在本 PRD 中展开。

## 10. 验收要点

### 10.1 首次进入

- 用户能看到活动标题、可用积分和领积分入口。
- 用户能理解首页包含赛事竞猜、积分抽奖、积分兑换、我的奖品入口。
- 无权限用户不能进入活动首页。

### 10.2 赛事竞猜

- 日期 Tab 只展示有比赛的日期。
- 当天无比赛时，不展示当天日期。
- 所有日期无比赛时，展示“暂无赛事安排”。
- 比赛竞猜截止时间为比赛日期前一日 `23:59:59`，比赛当天不可再竞猜。
- 研发只看赛事竞猜区说明，即可知道卡片字段、排序、状态和点击规则。
- 只有未参与且可竞猜的卡片能打开下注弹窗。
- 已参与、已截止未参与、待开奖、已猜中、已猜错、已取消/已退款状态不能重复下注。

### 10.3 我的竞猜记录

- 无记录时展示空态。
- 有记录时展示比赛日期、对阵双方、支持队伍、投入积分、当前结果状态。
- 未结算记录优先，已结算记录按结算时间倒序，已取消/已退款靠后。
- 记录只查看，不能编辑、撤回、改投或再次下注。

### 10.4 竞猜下注与结果

- 未选队伍、积分非正整数、低于最小值、超过余额、超过单场最大上限均不能提交，并展示明确提示。
- 投入积分默认取后台配置的最小下注积分，步进值默认等于最小下注积分。
- 提交成功后刷新积分、卡片状态和“今日竞猜一次”任务。
- 赛果出来后，用户能通过竞猜结果通知弹窗知道已有结果，并通过“我的竞猜记录”查看输赢和积分变化。

### 10.5 积分抽奖与奖品

- 抽奖区明确展示每次消耗积分。
- 奖品预览只展示奖品图片或占位图、奖品名称。
- 积分不足时 Toast 提示“当前积分不足”。
- 未中奖弹窗只展示“再抽一次”，点击空白遮罩可关闭。
- 用户最多中 1 个抽奖奖品，再次点击不扣积分并提示上限。

### 10.6 连中奖励

- 连续 N 场猜中后展示连中奖励弹窗。
- 弹窗标题、奖品名称和引导文案正确。
- 用户已获得过连中奖励后，不重复获得。
- 奖励进入“我的奖品”承接。

## 11. 待确认问题

- “我的奖品”组件由哪个已有页面或组件复用，入口路径和参数需要后续确认。
- 竞猜规则文案是否全部由后台配置，还是前台保留默认兜底文案。
- 抽奖奖品素材、奖品名称、发放说明是否由后台统一配置。
- 2.0 面向公开用户时，连中奖励是否改为积分或抽奖资格。

## 12. 需求变更点摘要

- 运行环境口径调整为微信小程序内嵌活动页，1.0 通过小程序 AppID 白名单控制访问。
- 首页标题调整为“赛事竞猜”。
- 赛事竞猜区补充日期 Tab 只展示有比赛日期的规则。
- 赛事竞猜区内收拢竞猜卡片字段、排序、状态和交互规则。
- 清理占位比赛卡片状态口径，未配置完整的比赛不在前台展示。
- 我的竞猜记录弹窗补充空态、字段、状态标签、排序和只读规则。
- 竞猜下注弹窗补充投入积分输入、后台配置映射、按钮状态、异常提示和提交反馈。
- 补充竞猜截止时间通用规则：比赛日期前一日 `23:59:59` 截止，比赛当天不可再竞猜。
- 竞猜结果通知能力调整为统一提醒，具体输赢和积分变化由“我的竞猜记录”承接。
- 积分抽奖区补充中奖公告、奖品预览、每次消耗积分、中奖和未中奖弹窗。
- 抽奖规则补充“本次活动每人最多中 1 个抽奖奖品”。
- 新增连中奖励弹窗，门槛 N 支持后台配置，默认 3。
- 新增我的奖品悬浮入口，用于承接抽奖中奖和连中奖励。
- 删除独立状态规则章节和偏技术化埋点明细，将规则收回到对应页面模块和弹窗中。
