# 景田赛事竞猜活动 PC 后台管理系统 PRD

## 1. 文档信息

| 项目 | 内容 |
| --- | --- |
| 文档名称 | 景田赛事竞猜活动 PC 后台管理系统 PRD |
| 版本 | V1.0 讨论稿 |
| 状态 | 待业务确认 |
| 创建日期 | 2026-05-07 |
| 最近更新 | 2026-05-07 |
| 适用端 | PC 后台管理系统 |
| 原型文件 | `admin.html` |
| 关联资料 | `项目背景说明.md`、`景田赛事竞猜活动-前台PRD.md` |

本次重构按后台系统类 PRD 结构整理，重点明确后台 V1 的模块边界、活动维度配置与比赛维度配置、赛果结算、取消退款、积分追溯、抽奖配置、连中奖励和统一奖品/奖励发放链路。

## 2. 项目背景

景田赛事竞猜活动前台优先运行在微信小程序内嵌活动页中，不再优先按 VB App 内嵌 H5 方案推进。前台活动页承接用户侧参与链路：

```text
做任务赚积分 -> 消耗积分参与赛事竞猜 -> 根据赛果结算积分 -> 用积分抽奖或进入积分商城兑换
```

当前前台原型已具备积分资产区、任务中心、赛事竞猜列表、竞猜记录、下注弹窗、结果弹窗、积分抽奖区、积分兑换入口，并新增连中奖励和“我的奖品”入口。

后台 V1 的定位不是完整营销中台，而是服务微信小程序内嵌活动运营场景下的轻量管理系统。1.0 公司内部试点阶段，后台需要支持通过小程序 AppID 白名单控制活动访问来源；2.0 如放开给公开用户使用，需另行补充公开用户访问、风控、奖品合规和流量承载方案。系统需要保障活动可配置、赛事可维护、赛果可结算、退款可追溯、积分可查询、奖品/奖励可发放。

## 3. 项目目标

### 3.1 业务目标

- 支持赛事竞猜活动从规则配置、赛事维护、用户参与、赛果结算到奖品发放的运营闭环。
- 保证竞猜扣分、猜中奖励、取消退款、抽奖消耗、人工调账等积分变化可追溯。
- 承接抽奖中奖记录和连中奖励记录，统一进入“我的奖品”和后台发放链路。
- 降低运营在赛事录入、赛果处理、退款、发放名单整理上的人工成本。

### 3.2 系统目标

- 后台菜单保持轻量，V1 仅保留必要运营模块。
- 活动级规则集中配置，避免每新增一场比赛重复配置通用规则。
- 赛果结算和退款重试必须幂等，避免重复发奖或重复退分。
- 后台不做复杂权限、审批、核销和数据大盘，减少 V1 开发复杂度。

## 4. 名词解释

| 名词 | 说明 |
| --- | --- |
| 活动规则配置 | 活动维度统一配置入口，包含下注门槛、奖励规则、同场下注限制、连中奖励和抽奖中奖上限等规则。 |
| 赛事 | 一场可供用户竞猜的比赛，由队伍、比赛开始时间、竞猜截止时间、前台展示状态等组成。 |
| 前台展示状态 | 控制赛事是否在小程序内嵌活动页展示，取值为显示/隐藏，不等同于赛事业务状态。 |
| 竞猜订单 | 用户对某场赛事选择支持队伍并投入积分后生成的记录。 |
| 积分流水 | 用户积分发生变动时生成的账务记录，需记录变动前余额、变动积分、变动后余额和关联业务。 |
| 赛果结算 | 录入胜出队伍后，系统按竞猜订单计算猜中返还、奖励发放和猜错积分沉淀。 |
| 预计发放奖励积分 | 赛果确认时预估给猜中用户额外发放的奖励积分，V1 为猜中用户下注本金合计。 |
| 预计收回积分 | 赛果确认时预估猜错用户投入且不会返还的积分合计。 |
| 连中奖励 | 用户连续 N 场“已参与且已结算”的竞猜猜中后获得的活动奖励。 |
| 奖品/奖励发放记录 | 后台统一承接抽奖中奖与连中奖励的发放记录。 |
| 我的奖品 | 小程序内嵌活动页用户侧奖品展示入口，统一展示抽奖中奖和连中奖励记录。 |

## 5. 项目范围

### 5.1 本期范围

后台 V1 包含 5 个一级模块：

1. 赛事管理。
2. 取消退款。
3. 积分账户。
4. 抽奖配置。
5. 中奖发放。

本期覆盖能力：

- 活动规则配置。
- 授权小程序 AppID 白名单配置。
- 赛事新增、编辑、隐藏、取消。
- 赛果录入、结算触发、结算失败幂等重试。
- 比赛取消后的积分退款和退款失败幂等重试。
- 用户积分账户查询、积分流水查询、人工调账。
- 抽奖基础配置、奖品配置、库存和概率配置。
- 抽奖中奖记录和连中奖励记录统一发放。
- 连中奖励名单导出。

### 5.2 非本期范围

V1 不做以下能力：

- 数据大盘。
- 操作审计独立页面。
- 多角色权限拆分。
- 审批流。
- 赛果双人确认。
- 兑换码。
- 二维码。
- 扫码核销。
- 通知状态。
- 积分商城商品和兑换订单管理。
- 单场覆盖活动规则。

说明：后台虽然不做独立审计页面，但后端仍需对关键操作保留必要留痕；所有积分变更必须生成积分流水。

## 6. 总体方案概览

### 6.1 信息架构

```text
景田赛事竞猜后台
├─ 赛事管理
│  ├─ 活动规则配置
│  ├─ 赛事列表
│  ├─ 新增/编辑赛事
│  ├─ 赛事详情
│  ├─ 录入赛果并结算
│  └─ 结算失败重试
├─ 取消退款
│  ├─ 退款批次列表
│  ├─ 退款批次详情
│  └─ 退款失败重试
├─ 积分账户
│  ├─ 用户积分账户查询
│  ├─ 积分流水查询
│  └─ 人工调账
├─ 抽奖配置
│  ├─ 抽奖基础配置
│  ├─ 奖品列表
│  ├─ 奖品新增/编辑
│  └─ 库存/概率配置
└─ 中奖发放
   ├─ 奖品/奖励发放记录
   ├─ 发放处理
   ├─ 批量标记发放
   └─ 连中奖励名单导出
```

### 6.2 主业务流程

1. 运营在赛事管理中配置活动规则。
2. 运营新增比赛，设置队伍、比赛开始时间、竞猜截止时间和前台展示状态。
3. 用户在微信小程序内嵌活动页参与竞猜，下注时扣减积分并生成竞猜订单和积分流水。
4. 竞猜截止后，运营录入赛果并触发结算。
5. 系统按竞猜订单生成结算结果和积分流水。
6. 系统判断用户是否达成连中奖励，达成后生成奖品/奖励记录并进入“我的奖品”。
7. 用户可继续使用积分抽奖，抽奖中奖记录进入统一奖品/奖励发放链路。
8. 运营在中奖发放模块查看、导出和标记发放状态。

### 6.3 活动维度配置与比赛维度配置

活动维度配置是整个活动统一生效的规则，不在每场赛事中重复配置。

| 配置维度 | 字段 | 说明 |
| --- | --- | --- |
| 活动维度 | 最小下注积分 | 默认 10。 |
| 活动维度 | 单场最大下注上限 | 可不填；不填时最大不超过用户当前可用积分。 |
| 活动维度 | 奖励规则 | V1 固定为猜中返还下注本金 + 等额奖励。 |
| 活动维度 | 同场下注限制 | 同一用户同一比赛只能下注一次，不支持加注、改投、撤回。 |
| 活动维度 | 授权小程序 AppID 白名单 | 1.0 公司内部试点用于限制活动入口来源；未在白名单内的小程序不可进入活动主流程。 |
| 活动维度 | 连中奖励配置 | 包含是否启用、连续猜中场次门槛、奖品 ID。 |
| 活动维度 | 抽奖中奖上限 | 同一用户本次活动所有奖品合计最多中 1 个。 |
| 比赛维度 | 队伍 A / 队伍 B | 单场比赛双方队伍。 |
| 比赛维度 | 比赛开始时间 | 比赛实际开始时间。 |
| 比赛维度 | 竞猜截止时间 | 用户可下注的截止时间，可早于比赛开始时间。 |
| 比赛维度 | 前台展示 | 控制小程序内嵌活动页是否展示该场赛事。 |

## 7. 模块详细方案

### 7.1 赛事管理

#### 7.1.1 模块目标

支持运营维护赛事、配置活动规则、录入赛果、触发结算，并处理结算失败重试。

#### 7.1.2 页面与入口

- 赛事管理列表页。
- 活动规则配置弹窗。
- 新增/编辑赛事弹窗。
- 赛事详情弹窗。
- 录入赛果并确认结算弹窗。
- 幂等重试结算弹窗。

#### 7.1.3 展现层

筛选区字段：

| 字段 | 说明 |
| --- | --- |
| 队伍 / 赛事 ID | 支持按队伍名或赛事 ID 搜索。 |
| 赛事状态 | 仅保留运营常用筛选项：全部状态、未开始、竞猜中、待处理、已结束、已取消。 |
| 前台展示 | 全部展示、显示、隐藏。 |
| 比赛日期 | 按比赛日期筛选。 |

筛选区操作：

- 查询。
- 重置。
- 活动规则配置。
- 新增赛事。

列表字段：

| 字段 | 说明 |
| --- | --- |
| 赛事 | 展示赛事 ID、队伍 A、队伍 B。 |
| 竞猜截止 | 展示竞猜截止时间。 |
| 比赛开始 | 展示比赛开始时间。 |
| 状态 | 展示赛事当前细分状态，如未开始、竞猜中、待录赛果、结算失败等。 |
| 前台展示 | 显示/隐藏。 |
| 参与人数 | 已下注用户数。 |
| 投入积分 | 当前赛事累计投入积分。 |
| 操作 | 详情、编辑、取消、录赛果、重试结算等。 |

状态筛选映射：

| 筛选项 | 包含细分状态 |
| --- | --- |
| 未开始 | 未开始。 |
| 竞猜中 | 竞猜中。 |
| 待处理 | 已截止、待录赛果、结算中、结算失败、部分失败。 |
| 已结束 | 已结算。 |
| 已取消 | 已取消、退款中、已退款。 |

#### 7.1.4 表单与校验

活动规则配置弹窗分为两个区域。

竞猜规则：

| 字段 | 类型 | 规则 |
| --- | --- | --- |
| 授权小程序 AppID 白名单 | 多行文本/列表 | 1.0 必填；支持录入一个或多个小程序 AppID；仅白名单内小程序可承接活动入口。 |
| 最小下注积分 | 数字输入 | 默认 10；需为正整数。 |
| 单场最大下注上限 | 数字输入 | 可不填；填写时需为不小于最小下注积分的正整数；不要求是 10 的倍数。 |
| 奖励规则 | 只读展示 | 猜中返还下注本金 + 等额奖励。 |
| 同场下注限制 | 只读展示 | 同一用户同一比赛只能下注一次。 |

连中奖励：

| 字段 | 类型 | 规则 |
| --- | --- | --- |
| 是否启用 | 下拉/开关 | 启用、停用。 |
| 连续猜中场次门槛 | 数字输入 | 默认 3；需为正整数。 |
| 奖品 ID | 文本输入 | 必填；用于关联奖品系统中的具体奖品。 |

新增/编辑赛事弹窗只保留比赛维度字段：

| 字段 | 类型 | 规则 |
| --- | --- | --- |
| 队伍 A | 文本输入 | 必填。 |
| 队伍 B | 文本输入 | 必填；不能与队伍 A 相同。 |
| 比赛开始时间 | 日期时间 | 必填。 |
| 竞猜截止时间 | 日期时间 | 必填；不能晚于比赛开始时间。 |
| 前台展示 | 单选/下拉 | 显示、隐藏。 |

表单提示：

- 下注门槛、奖励规则、连中奖励等统一在活动规则配置中管理。
- 已有下注后，仅可修改前台展示等不影响下注和结算口径的字段。

#### 7.1.5 交互层

- 点击查询：按筛选条件刷新赛事列表。
- 点击重置：清空筛选条件并恢复默认列表。
- 点击活动规则配置：打开活动规则配置弹窗。
- 点击新增赛事：打开新增赛事弹窗。
- 点击编辑：未产生下注时可编辑比赛关键字段；已有下注后限制关键字段。
- 点击详情：查看赛事基础信息、参与数据、赛果和结算状态。
- 点击录赛果：在当前页打开录入赛果弹窗，不跳转独立页面。
- 点击重试结算：打开幂等重试结算弹窗。

#### 7.1.6 业务规则

- V1 固定赔率为 1:1，后台不提供赔率配置入口。
- 猜中用户到账：下注本金 + 等额奖励。
- 猜错用户：下注积分不返还，计入预计收回积分。
- 同一用户同一比赛只能下注一次。
- 前台展示状态只控制小程序内嵌活动页是否展示，不改变赛事业务状态。
- 隐藏赛事不影响已下注用户订单和后续结算。
- 赛事取消后不再允许录入赛果，只进入取消退款流程。

#### 7.1.7 状态与异常

赛事核心状态：

```text
未开始 -> 竞猜中 -> 已截止 -> 待录赛果 -> 结算中 -> 已结算
```

异常状态：

```text
结算中 -> 结算失败/部分失败 -> 重试结算 -> 已结算/失败待处理
```

取消分支：

```text
未开始/竞猜中/已截止 -> 已取消 -> 退款中 -> 已退款
```

结算失败重试要求：

- 仅对失败或未完成结算记录生效。
- 已成功结算记录不可重复发奖。
- 重试结果需更新结算任务状态和失败原因。

### 7.2 取消退款

#### 7.2.1 模块目标

处理比赛取消后的用户下注本金退回，并支持失败退款重试。

#### 7.2.2 页面与入口

- 退款批次列表。
- 退款批次详情。
- 异常退款处理弹窗。

#### 7.2.3 展现层

退款批次列表字段：

| 字段 | 说明 |
| --- | --- |
| 退款批次 ID | 系统生成的退款批次编号。 |
| 赛事 | 被取消的赛事。 |
| 退款状态 | 待退款、退款中、已退款、退款失败、部分失败。 |
| 应退人数 | 已下注用户数。 |
| 应退积分 | 需退回的下注本金合计。 |
| 成功/失败 | 成功笔数和失败笔数。 |
| 创建时间 | 退款批次创建时间。 |
| 操作 | 详情、重试。 |

退款批次详情字段：

- 用户 ID。
- 员工姓名。
- 部门。
- 手机号。
- 下注积分。
- 退款积分。
- 原下注流水 ID。
- 退款流水 ID。
- 处理状态。
- 失败原因。

#### 7.2.4 业务规则

- 仅比赛取消触发退款。
- 只退回用户下注本金，不发放奖励积分。
- 每笔退款必须关联原下注流水。
- 退款重试必须幂等，不能重复退分。
- 退款完成后赛事取消分支状态更新为已退款。

#### 7.2.5 状态与异常

退款状态：

```text
待退款 -> 退款中 -> 已退款
```

异常状态：

```text
退款中 -> 退款失败/部分失败 -> 重试中 -> 已退款/失败待处理
```

### 7.3 积分账户

#### 7.3.1 模块目标

支持运营查询用户积分账户、追溯积分流水，并进行必要的人工调账。

#### 7.3.2 页面与入口

- 用户积分账户查询。
- 积分流水列表。
- 流水详情。
- 人工调账弹窗。
- 人工调账记录。

#### 7.3.3 展现层

账户查询字段：

| 字段 | 说明 |
| --- | --- |
| 员工信息 | 支持按员工 ID、姓名、手机号查询。 |
| 当前可用积分 | 用户当前可用积分。 |
| 累计获得 | 用户累计获得积分。 |
| 累计消耗 | 用户累计消耗积分。 |

积分流水字段：

| 字段 | 说明 |
| --- | --- |
| 流水 ID | 积分流水编号。 |
| 用户 | 员工 ID、姓名、部门、手机号。 |
| 业务类型 | 积分变动来源。 |
| 变动前余额 | 积分变动前用户余额。 |
| 变动积分 | 正数为增加，负数为扣减。 |
| 变动后余额 | 积分变动后用户余额。 |
| 关联业务单号 | 竞猜订单、退款批次、抽奖记录、调账单等。 |
| 时间 | 流水生成时间。 |

#### 7.3.4 表单与校验

人工调账字段：

| 字段 | 类型 | 规则 |
| --- | --- | --- |
| 员工 ID | 输入/查询 | 必填；需匹配有效员工。 |
| 员工信息 | 只读展示 | 展示姓名、部门、手机号、当前积分。 |
| 调账类型 | 单选 | 人工加分、人工扣分。 |
| 调账积分 | 数字输入 | 必填；正整数。 |
| 调账原因 | 文本输入 | 必填。 |

#### 7.3.5 业务规则

- 人工调账不走审批。
- 人工调账提交前需二次确认。
- 人工调账必须生成积分流水。
- 人工调账记录需保留操作人、操作时间和原因。

积分流水类型：

```text
任务奖励
竞猜下注扣减
竞猜猜中奖励
比赛取消退款
抽奖消耗
人工加分
人工扣分
```

### 7.4 抽奖配置

#### 7.4.1 模块目标

支持运营配置抽奖基础规则、奖品、库存、概率，并同步抽奖中奖上限规则。

#### 7.4.2 页面与入口

- 抽奖基础配置。
- 奖品列表。
- 奖品新增/编辑。
- 概率配置。
- 库存变更记录。

#### 7.4.3 展现层

抽奖基础配置字段：

| 字段 | 说明 |
| --- | --- |
| 活动开关 | 控制抽奖是否启用。 |
| 单次消耗积分 | 用户每次抽奖消耗积分。 |
| 每日次数限制 | 单用户每日可抽奖次数。 |
| 总次数限制 | 单用户活动期总抽奖次数。 |
| 中奖上限提示 | 每人本次活动最多中奖 1 次，已中奖后再次抽奖不扣积分。 |

奖品列表字段：

| 字段 | 说明 |
| --- | --- |
| 奖品 ID | 奖品唯一标识。 |
| 奖品名称 | 前台展示名称。 |
| 奖品类型 | 实物、积分、谢谢参与等。 |
| 库存 | 当前可用库存。 |
| 已中奖数 | 已生成中奖记录数量。 |
| 概率 | 抽中概率。 |
| 状态 | 启用、停用。 |
| 操作 | 编辑、库存调整等。 |

#### 7.4.4 表单与校验

奖品新增/编辑字段：

| 字段 | 类型 | 规则 |
| --- | --- | --- |
| 奖品 ID | 文本输入 | 必填；需唯一。 |
| 奖品名称 | 文本输入 | 必填。 |
| 奖品类型 | 下拉 | 实物、积分、谢谢参与等。 |
| 库存 | 数字输入 | 非负整数。 |
| 概率 | 数字输入 | 需满足概率配置总和规则。 |
| 状态 | 下拉 | 启用、停用。 |

#### 7.4.5 业务规则

- 用户抽奖前扣减积分。
- 同一用户本次活动所有奖品合计最多中 1 个。
- 未中奖用户可继续抽奖。
- 已中奖用户再次抽奖时不扣积分，并提示已达中奖上限。
- 命中奖品库存充足时，生成中奖记录并扣减库存。
- 命中奖品库存为 0 时，不得超发；按产品口径落到“谢谢参与”或重新抽取可用奖品。
- 必须保留“谢谢参与”兜底项。

### 7.5 中奖发放

#### 7.5.1 模块目标

统一承接抽奖中奖和连中奖励记录，支持运营查看、导出和处理奖品/奖励发放状态，并为小程序内嵌活动页“我的奖品”组件提供记录来源。

#### 7.5.2 页面与入口

- 奖品/奖励发放记录列表。
- 发放处理弹窗。
- 批量标记发放弹窗。
- 导出连中奖励名单。

#### 7.5.3 展现层

列表字段：

| 字段 | 说明 |
| --- | --- |
| 记录 ID | 中奖或奖励记录编号。 |
| 奖励来源 | 抽奖中奖、连中奖励。 |
| 用户资料 | 员工 ID、姓名、部门、手机号。 |
| 奖品/奖励 | 奖品名称或奖励名称。 |
| 获得时间 | 中奖或达成奖励的时间。 |
| 达成场次 | 仅连中奖励展示，如 3 连中；抽奖中奖展示为“-”。 |
| 发放状态 | 待发放、已发放、发放失败、无需发放。 |
| 操作 | 详情、处理发放。 |

发放处理字段：

| 字段 | 说明 |
| --- | --- |
| 中奖/奖励记录 | 记录 ID、奖励来源。 |
| 奖品/奖励 | 奖品 ID、名称、类型。 |
| 用户 | 员工姓名、部门、手机号。 |
| 发放状态 | 待发放、已发放、发放失败、无需发放。 |
| 处理人 | 当前后台操作人。 |
| 发放备注 | 运营填写的处理备注。 |

#### 7.5.4 业务规则

- 抽奖中奖和连中奖励进入同一发放列表。
- “我的奖品”组件统一承接抽奖中奖和连中奖励记录。
- 连中奖励名单支持导出。
- V1 不展示兑换码、二维码、扫码核销信息。
- V1 不做通知状态。
- 实物奖品用户资料优先读取微信小程序授权用户资料；1.0 公司内部试点需结合小程序 AppID 白名单和内部用户名单确认用户身份。

#### 7.5.5 发放状态

```text
待发放 -> 已发放
待发放 -> 发放失败 -> 重新处理/已发放
无需发放
```

## 8. 跨模块规则

### 8.1 积分规则

- 竞猜下注成功后立即扣减用户积分。
- 猜中用户结算到账：下注本金 + 等额奖励。
- 猜错用户下注积分不返还。
- 比赛取消只退回下注本金，不发奖励。
- 所有积分变更必须生成积分流水。
- 积分流水需能关联原始业务单号。

### 8.2 连中奖励规则

- 连中奖励属于活动维度配置。
- 只统计用户已参与且已结算的竞猜记录。
- 猜中累计连中次数。
- 猜错清零。
- 未参与比赛不打断连中。
- 比赛取消/退款不计入连中，也不打断连中。
- 每个用户每次活动最多获得 1 次连中奖励。
- 达成后按奖品 ID 生成奖品/奖励记录，并进入“我的奖品”链路。

### 8.3 幂等规则

- 结算重试只处理失败或未完成的结算明细。
- 已成功结算用户不可重复发放积分。
- 退款重试只处理失败或未完成的退款明细。
- 已成功退款用户不可重复退分。
- 连中奖励发放判断需避免同一用户重复生成奖励记录。
- 抽奖中奖上限需在扣积分前判断，已中奖用户再次抽奖不扣积分。

### 8.4 留痕规则

- 后台 V1 不做独立操作审计页面。
- 取消比赛、录入赛果、结算重试、退款重试、人工调账、修改抽奖概率、修改库存、奖品发放等关键操作，后端需保留操作人、操作时间、业务对象和操作结果。
- 所有积分变更类操作必须同时有积分流水。

## 9. 风险与边界

- 赛果结算涉及积分发放，需优先保证幂等和流水一致性。
- 取消退款需关联原下注流水，避免无法解释用户积分来源。
- 连中奖励当前为内部活动奖励，若后续扩展到更大范围用户，需要重新评估实物奖励表达和履约方式。
- 抽奖库存为 0 时必须避免超发，库存判断需与中奖记录生成保持一致。
- “我的奖品”组件需要识别奖励来源，避免只支持抽奖中奖而遗漏连中奖励。
- 后台 V1 不管理积分商城，积分商城相关商品、订单、兑换流程由现有组件承接。

## 10. 待确认问题

1. 连中奖励的奖品 ID 是否已在奖品系统中固定，是否需要后台做奖品 ID 有效性校验。
2. 抽奖命中库存为 0 的奖品时，最终采用“落到谢谢参与”还是“重新抽取可用奖品”。
3. 奖品/奖励发放失败后，是否需要区分失败原因类型，还是 V1 仅保留文本备注。
4. “我的奖品”组件读取记录时，是否需要统一字段协议，例如奖励来源、奖品 ID、展示名称、发放状态。

## 11. 需求变更摘要

- 按后台系统类 PRD 结构重构文档，不再保留单独检查清单章节。
- 后台信息架构收敛为 5 个一级模块：赛事管理、取消退款、积分账户、抽奖配置、中奖发放。
- 明确活动维度配置和比赛维度配置边界。
- 新增活动规则配置，将最小下注、单场最大下注上限、奖励规则、同场下注限制、连中奖励统一收拢。
- 连中奖励配置字段改为奖品 ID，不再使用奖励名称作为配置字段。
- 中奖发放模块统一承接抽奖中奖和连中奖励，并明确不做通知状态、兑换码、二维码和扫码核销。
- 同步抽奖中奖上限规则：同一用户本次活动所有奖品合计最多中 1 个，已中奖后再次抽奖不扣积分。
