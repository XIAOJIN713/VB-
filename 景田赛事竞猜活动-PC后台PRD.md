# 景田赛事竞猜活动 PC 后台管理系统 PRD

## 1. 文档信息

- 版本：V1.1
- 状态：后台管理系统设计稿
- 对应原型：[admin.html](admin.html)
- 本次调整：移除“数据大盘”和“操作审计”独立模块，简化后台范围，强化关键业务弹窗和操作流程。

## 2. 项目定位

景田赛事竞猜活动运行在景田集团内部 VB App 中，前台 H5 负责用户侧展示和参与，PC 后台负责赛事、赛果、退款、积分、抽奖和中奖发放等运营管理。

核心链路：

```text
做任务赚积分 -> 消耗积分参与赛事竞猜 -> 根据赛果结算积分 -> 用积分抽奖或进入积分商城兑换
```

V1 后台只做必要运营管理，不做：

- 数据大盘
- 操作审计独立页面
- 积分商城商品和兑换订单管理
- 后台多角色权限拆分
- 审批流

说明：虽然不做“操作审计”独立页面，但敏感操作仍需在后端留痕，便于后续追溯。

## 3. 后台信息架构

后台一级导航调整为 6 个模块：

1. 赛事管理
2. 赛果结算
3. 取消退款
4. 积分账户
5. 抽奖配置
6. 中奖发放

默认进入“赛事管理”。

## 4. 核心业务规则

### 4.1 竞猜规则

- 猜中奖励 = 下注本金 + 奖励积分。
- 奖励积分 = 投入积分 × 赔率。
- V1 默认赔率为 `1:1`。
- 示例：用户下注 100 分，猜中后到账 100 本金 + 100 奖励，共 200 分。
- 猜错后下注积分不返还。
- 同一用户同一比赛只能下注一次。
- 不支持加注、改投、撤回。
- 全局最小下注积分为 10 分。
- 最大下注积分不能超过用户当前可用积分。
- 后台预留“单场最大下注上限”字段。
- 比赛开始时间和竞猜截止时间分开配置。
- 比赛开始后或竞猜截止后，用户不可继续下注。

### 4.2 赛果结算规则

- 赛果由后台运营单人录入。
- 不做双人审批。
- 录入赛果后必须二次确认。
- 二次确认弹窗需展示胜方、竞猜分布、预计返还本金、预计奖励积分、失败方消耗积分。
- 结算失败允许手动重试。
- 重试必须幂等，只处理失败或未完成记录。
- 已成功结算用户不能重复发奖。

### 4.3 取消退款规则

- 仅比赛取消触发退款。
- 取消比赛后，所有已下注用户全额退回下注本金。
- 取消比赛不发奖励积分。
- 每笔退款必须关联原下注流水。
- 退款失败允许重试。
- 退款重试必须幂等，已退款用户不能重复退分。

### 4.4 人工调账规则

- 人工调账不走审批。
- 可按员工 ID 进行加分或扣分。
- 必须填写调账原因。
- 必须生成积分流水。
- 必须保留操作人、操作时间、操作原因等后端留痕信息。

### 4.5 抽奖规则

- 用户抽奖前先扣减积分。
- 后台配置单次消耗积分、奖品、库存、概率。
- 命中奖品且库存充足时，生成中奖记录并扣减库存。
- 命中奖品库存为 0 时，不得超发。
- 库存为 0 的命中结果应落到“谢谢参与”或重新抽取可用奖品。
- 必须保留“谢谢参与”兜底项。
- 实物奖品发放信息优先来自 VB App 用户资料。

## 5. 页面需求

### 5.1 赛事管理

页面组成：

- 赛事列表
- 新增赛事弹窗
- 编辑赛事弹窗
- 赛事详情弹窗
- 取消比赛确认弹窗

列表字段：

- 赛事 ID
- 对阵队伍
- 竞猜截止时间
- 比赛开始时间
- 赛事状态
- 参与人数
- 投入积分
- 操作

新增/编辑字段：

- 队伍 A
- 队伍 B
- 比赛开始时间
- 竞猜截止时间
- 赔率
- 单场最大下注上限
- 展示状态
- 最小下注积分，V1 固定 10 分

编辑限制：

- 草稿状态可自由编辑。
- 已产生下注后，不允许修改队伍、赔率、竞猜截止时间等关键字段。
- 已截止、比赛中、待录赛果、结算中、已结算状态下，不允许编辑关键字段。

取消比赛弹窗需展示：

- 影响用户数
- 应退积分
- 退款规则
- 取消原因输入框
- 高风险提示

### 5.2 赛果结算

页面组成：

- 待录入赛果列表
- 赛果录入弹窗
- 结算失败重试弹窗

赛果录入弹窗需包含：

- 赛事信息
- 胜出队伍选择
- 备注
- 胜方人数
- 败方人数
- 预计返还本金
- 预计奖励积分
- “确认赛果并结算”按钮

结算重试弹窗需包含：

- 成功记录数
- 失败记录数
- 重试次数
- 失败原因示例
- 幂等重试说明

### 5.3 取消退款

页面组成：

- 退款批次列表
- 退款批次详情弹窗
- 退款失败重试弹窗

退款批次列表字段：

- 退款批次 ID
- 赛事
- 状态
- 应退人数
- 应退积分
- 操作

退款详情弹窗需展示：

- 应退人数
- 应退积分
- 退款状态
- 用户
- 下注积分
- 原下注流水
- 退款流水

退款重试弹窗需展示：

- 本次重试范围
- 幂等重试说明
- 重试备注输入框

### 5.4 积分账户

页面组成：

- 用户积分账户查询
- 积分流水列表
- 人工调账卡片
- 人工调账确认弹窗

用户账户字段：

- 员工 ID
- 姓名
- 部门
- 手机号
- 当前可用积分
- 累计获得积分
- 累计消耗积分

积分流水类型：

```text
任务奖励
竞猜下注扣减
竞猜猜中奖励
比赛取消退款
抽奖消耗
人工加分
人工扣分
```

人工调账确认弹窗需包含：

- 员工 ID
- 员工信息
- 调账类型
- 调账积分
- 调账原因
- 确认调账按钮

### 5.5 抽奖配置

页面组成：

- 抽奖基础配置
- 奖品与概率列表
- 新增/编辑奖品弹窗

基础配置字段：

- 单次消耗积分
- 每日次数限制
- 总次数限制

奖品字段：

- 奖品名称
- 奖品类型：实物、虚拟、谢谢参与
- 库存
- 已中奖数
- 中奖概率
- 状态

奖品弹窗需包含：

- 奖品名称
- 奖品类型
- 库存
- 中奖概率
- 发放说明
- 库存为 0 不得超发提示

### 5.6 中奖发放

页面组成：

- 中奖记录列表
- 奖品发放处理弹窗
- 批量发放弹窗

中奖记录字段：

- 中奖记录 ID
- 用户资料
- 奖品
- 中奖时间
- 发放状态
- 操作

发放处理弹窗需包含：

- 中奖记录
- 奖品
- 用户
- 手机号
- 发放状态
- 处理人
- 发放备注

发放状态：

```text
待发放 -> 已发放
待发放 -> 发放失败 -> 重新处理/已发放
无需发放
```

## 6. 状态流转

### 6.1 赛事状态

```text
草稿 -> 已发布 -> 竞猜中 -> 已截止 -> 比赛中 -> 待录入赛果 -> 赛果已录入 -> 结算中 -> 已结算
```

取消分支：

```text
已发布/竞猜中/已截止/比赛中 -> 已取消 -> 退款中 -> 已退款
```

### 6.2 竞猜订单状态

```text
已下注/待赛果 -> 猜中/待发奖 -> 已发奖
已下注/待赛果 -> 猜错/已结算
已下注/待赛果 -> 比赛取消/待退款 -> 已退款
```

### 6.3 结算状态

```text
未结算 -> 赛果已录入 -> 结算中 -> 结算成功
```

异常分支：

```text
结算中 -> 部分成功/结算失败 -> 幂等重试中 -> 结算成功/失败待处理
```

### 6.4 退款状态

```text
无需退款 / 待退款 -> 退款中 -> 已退款
```

异常分支：

```text
退款中 -> 部分退款失败/退款失败 -> 重试中 -> 已退款/失败待处理
```

## 7. 验收场景

- 后台默认进入赛事管理，不展示数据大盘。
- 左侧导航不展示操作审计。
- 新增赛事弹窗包含赛事字段和下注规则字段。
- 已有下注赛事编辑弹窗能提示关键字段不可修改。
- 取消比赛弹窗展示影响用户数、应退积分和取消原因。
- 录入赛果弹窗展示胜方选择和预计结算数据。
- 结算重试弹窗明确只处理失败记录，不重复发奖。
- 退款详情弹窗展示原下注流水和退款流水。
- 退款重试弹窗明确只处理失败退款记录。
- 人工调账弹窗要求填写员工、积分和原因。
- 奖品弹窗包含库存、概率和发放说明。
- 奖品库存为 0 时页面明确提示不得超发。
- 中奖发放弹窗展示用户资料、发放状态和发放备注。

## 8. V1 不做范围

- 不做数据大盘。
- 不做操作审计独立页面。
- 不做后台多角色权限拆分。
- 不做调账审批流。
- 不做赛果双人审批。
- 不做积分商城商品管理。
- 不做积分商城兑换订单管理。
